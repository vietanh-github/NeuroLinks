version: "3.9"

services:
  bot:
    build: .
    container_name: neurolinks-bot
    restart: unless-stopped
    env_file: .env
    volumes:
      # Makes service account key available inside the container
      - ./${FIREBASE_SERVICE_ACCOUNT_PATH}:/app/${FIREBASE_SERVICE_ACCOUNT_PATH}:ro
